# CSC212-project
CSC212 Project prefroms boolean queries on a set of documents on a CSV file using fast search methods such as binary tree. The search methods are not imported from libraries but rather handbuilt step by step
Requirements: Java latest version installed.
Preferences: an IDE to run the code, communicate with the application, and show results on console. 
